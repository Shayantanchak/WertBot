name: wertbot_mobile
description: WertBot mobile application — AI-powered personal finance and HFT trading co-pilot.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^2.4.1
  fl_chart: ^0.66.0
  lucide_icons: ^0.320.0
  provider: ^6.1.1
  google_fonts: ^6.1.0
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.1.8
  flutter_markdown: ^0.6.18

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
